import 'package:flutter/material.dart';

class page_home extends StatelessWidget {
  const page_home({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Esta es la paguina Home'),

    );
  }
}