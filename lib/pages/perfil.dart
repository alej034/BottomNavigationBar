import 'package:flutter/material.dart';

class page_perfil extends StatelessWidget {
  const page_perfil({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Esta es la paguina Perfil'),

    );
  }
}